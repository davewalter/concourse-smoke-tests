---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: golang

inputs:
- name: concourse-smoke-tests
- name: concourse-vars

params:
  FLY_SKIP_SSL: true

run:
  path: concourse-smoke-tests/ci/tasks/run-tests
